# Forecasting-only dependencies. Install on top of backend/requirements.txt.
#
# Torch is pinned to the CPU-only wheels. On a fresh install run:
#
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r backend/forecasting/requirements.txt
#
# torch is intentionally NOT listed below to keep the CPU-only resolver clean.

statsmodels>=0.14,<1
chronos-forecasting>=1.4,<2
transformers>=4.40,<5
accelerate>=0.30,<1
pyarrow>=15,<22
pytest>=8,<9
matplotlib>=3.8,<4
